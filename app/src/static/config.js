window.APP_CONFIG = {
    // Optional: URL of deployed Skylink2 bridge backend, e.g. "https://bridge.example.com"
    // Leave empty to call the hosted model endpoint directly from browser.
    BRIDGE_BASE_URL: ""
};
